{
  "name": "blog",
  "version": "1.0.0",
  "author": "Ress <git@x39.dev>",
  "license": "MIT",
  "scripts": {
    "build": "hugo --minify",
    "dev": "hugo server --disableLiveReload",
    "server": "hugo server --minify --disableLiveReload",
    "deploy": "firebase deploy --token $FIREBASE_TOKEN"
  },
  "devDependencies": {
    "firebase-tools": "*"
  }
}
